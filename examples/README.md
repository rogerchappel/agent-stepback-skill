# Examples

Run:

```bash
node ../bin/agent-stepback.js stuck-run.md --format markdown --max-items 2
```

The example includes a failure, an assumption, and a next action so every major section has signal.
